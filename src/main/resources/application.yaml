spring:
  application:
    name: employee-management-system

  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/ems

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
